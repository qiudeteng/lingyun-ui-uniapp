# Git 提交说明（源代码管理 ✨ 自动生成时必须遵守）

在**源代码管理**侧边栏点击 sparkle 自动生成 commit message，格式如下：

```
{类型}：{简要描述}

任务清单：
1. 升级了某个依赖
2. 修复了某个 bug
3. 添加了某个功能
4. 优化了某个性能
5. 重构了某个模块
6. 添加了某个文档
7. 添加了某个测试
8. 添加了某个配置
9. 添加了某个优化
10. 添加了某个删除
```

## 硬性要求

- 仅使用**简体中文**
- **简要描述不超过 200 个字符**（含类型与全角冒号）
- 使用**全角冒号** `：`，禁止半角 `:`
- 禁止 Conventional Commits（禁止 `feat:`、`fix:`、`chore:` 等）
- 禁止多行、禁止前缀说明、禁止英文

## 类型（只能选一个）

新增 | 修复 | 更新 | 重构 | 文档 | 测试 | 配置 | 优化 | 删除

| 类型 | 用途 |
|------|------|
| 新增 | 新功能、新页面 |
| 修复 | Bug 修复 |
| 更新 | 功能增强、依赖升级 |
| 重构 | 结构调整 |
| 文档 | 文档变更 |
| 测试 | 测试相关 |
| 配置 | 工具链、CI、环境 |
| 优化 | 性能、体验 |
| 删除 | 移除内容 |

## 示例（照此风格输出）

```
新增：QC 桥调试页 wsRequest 用例
任务清单：
1. 新增了 QC 桥调试页 wsRequest 用例
修复：WebView 导航页在窄屏下显示空白
任务清单：
1. 修复了 WebView 导航页在窄屏下显示空白
配置：添加 commitlint 与 husky 提交校验
任务清单：
1. 添加了 commitlint 与 husky 提交校验
更新：接入 ECharts 并展示工作台图表
任务清单：
1. 更新了接入 ECharts 并展示工作台图表
```
