# Skeletons 规范（强制 · lingyun-skeleton）

加载占位 · opacity 脉冲（mp 友好，无 `*` 选择器）。

| 属性 | 默认 | 说明 |
|------|------|------|
| `rows` | `3` | 正文行数 |
| `avatar` | `false` | 左侧圆头像位 |
| `title` | `true` | 标题短条 |

```vue
<lingyun-skeleton :rows="4" avatar />
```
